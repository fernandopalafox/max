# 2026-04-30

## OGD Full Dynamics Adaptation (dynamics_sft_pt)

Testing online adaptation of the full dense dynamics head using pure consistency loss (OGD trainer) instead of the full TDMPC2 world-model loss. Each run loads a different pretrained dense baseline and adapts only the dynamics head to cheetah mass scale 0.1.

| Run name | GPU | Pretrained model |
|----------|-----|-----------------|
| dynamics_sft_pt_r3 | 0 | baseline_new/run_3 |
| dynamics_sft_pt_r4 | 1 | baseline_new/run_4 |
| dynamics_sft_pt_r5 | 2 | baseline_new/run_5 |
| dynamics_sft_pt_r6 | 3 | baseline_new/run_6 |

## TDMPC2-Frozen Full Dynamics Adaptation (dynamics_pt)

Same setup as above but using the tdmpc2frozen trainer (consistency + reward + value losses) instead of OGD. Direct comparison to dynamics_sft_pt to isolate the effect of the training signal. Runs after dynamics_sft_pt completes.

| Run name | GPU | Pretrained model |
|----------|-----|-----------------|
| dynamics_pt_r3 | 0 | baseline_new/run_3 |
| dynamics_pt_r4 | 1 | baseline_new/run_4 |
| dynamics_pt_r5 | 2 | baseline_new/run_5 |
| dynamics_pt_r6 | 3 | baseline_new/run_6 |

## OGD LoRA-XS Adaptation from Dense Baseline (loraxs_from_dense_sft)

Fresh LoRA-XS adapters on top of `baseline_new` pretrained models, adapted with OGD trainer. Same pretrained models as dynamics_sft_pt but testing whether the LoRA-XS subspace + OGD is better than full dynamics + OGD.

| Run name | GPU | Pretrained model |
|----------|-----|-----------------|
| loraxs_from_dense_sft_r3 | 0 | baseline_new/run_3 |
| loraxs_from_dense_sft_r4 | 1 | baseline_new/run_4 |
| loraxs_from_dense_sft_r5 | 2 | baseline_new/run_5 |
| loraxs_from_dense_sft_r6 | 3 | baseline_new/run_6 |

Config: `adapt_full_with_loraxs_pt_sft.json`. Runs after dynamics_pt completes.

## OGD LoRA-XS Adaptation from LoRA-XS Baseline (loraxs_sft_pt)

Pretrained LoRA-XS adapters from `baseline_loraxs`, adapted with OGD trainer. Tests whether a pretrained LoRA-XS subspace + OGD outperforms tdmpc2frozen adaptation.

| Run name | GPU | Pretrained model |
|----------|-----|-----------------|
| loraxs_sft_pt_r3 | 0 | baseline_loraxs/run_3 |
| loraxs_sft_pt_r4 | 1 | baseline_loraxs/run_4 |
| loraxs_sft_pt_r5 | 2 | baseline_loraxs/run_5 |
| loraxs_sft_pt_r6 | 3 | baseline_loraxs/run_6 |

Config: `adapt_loraxs_pt_sft.json`. Runs after loraxs_from_dense_sft completes.
